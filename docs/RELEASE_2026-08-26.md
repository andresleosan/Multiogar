# Release 2026-08-26

## Alcance

- Inicio de sesion general con Google y correo/contrasena.
- Navegacion publica hacia el acceso general y proteccion de rutas administrativas.
- Chat publico mediante Route Handlers con identidad temporal HMAC, validacion, autorizacion por conversacion y rate limiting transaccional.
- Reglas de Firestore sin permisos administrativos por correo; el personal requiere custom claims.
- Autenticacion de Firebase Admin mediante Vercel OIDC y credenciales temporales.
- Gestion de permisos del equipo para superadmin, vendedor y cliente desde `/admin/usuarios`, protegida por Firebase Admin y rate limiting.

No hay migraciones ni eliminacion de documentos en este release.

## Evidencia previa

- `pnpm test`: 23/23.
- `pnpm test:rules`: 4/4 con Firestore Emulator Suite.
- `pnpm lint`: limpio.
- `tsc --noEmit`: limpio.
- `pnpm build`: 33 paginas y 3 Route Handlers.
- `pnpm audit --prod`: una alerta moderada transitiva en `uuid@9.0.1`. Los consumidores detectados (`gaxios` y `teeny-request`) invocan `uuid.v4`; la vulnerabilidad reportada afecta v3/v5/v6 cuando reciben un buffer. No se fuerza una version mayor porque `gaxios@6.7.1` revirtio previamente ese cambio de dependencia.

## Reglas desplegadas

- Ruleset activo: `projects/multiogarweb/rulesets/065ce695-39c4-4596-9d7e-474b70d4a305`.
- Compilacion remota de Firebase Rules: correcta.
- Prueba negativa en produccion: `admin@admin.com`, sin custom claim, recibe HTTP `403` al leer `orders`.

## Identidad OIDC de produccion

- Proyecto GCP: `multiogarweb` (`311713625539`).
- Cuenta de servicio: `multiogar-vercel-chat@multiogarweb.iam.gserviceaccount.com`.
- Permiso concedido: `roles/datastore.user` a nivel de proyecto.
- Pool/proveedor: `projects/311713625539/locations/global/workloadIdentityPools/vercel/providers/vercel`.
- Issuer: `https://oidc.vercel.com/andres-leo-san-s-projects`.
- Audiencia permitida: `https://vercel.com/andres-leo-san-s-projects`.
- Condicion: propietario `andres-leo-san-s-projects`, proyecto `multiogar`, entorno `production`.
- Impersonacion permitida unicamente para `owner:andres-leo-san-s-projects:project:multiogar:environment:production`.
- Variables `GCP_*` configuradas solo en Vercel Production; no se almaceno ninguna clave privada.
- Deployment final: `dpl_AusjfdeQD1rQbHa9GxXbtuXRTN4j` (`READY`, Production), alias `https://multiogar.vercel.app`.

## Rollback

- Commit de aplicacion previo: `f5b1d58`.
- Ruleset activo previo: `projects/multiogarweb/rulesets/6a9cc14b-7f73-4bd5-909f-93259aa1a14d`.
- Release Firestore: `projects/multiogarweb/releases/cloud.firestore`.

Para revertir reglas, volver a apuntar el release `cloud.firestore` al ruleset previo mediante Firebase Rules API o restaurarlo desde el historial de Rules en Firebase Console. Para revertir la aplicacion, promover en Vercel el deployment asociado al commit previo. Ninguno de estos rollbacks modifica datos.

## Validacion de produccion

- Smoke test 2026-08-26: home `200`, login `200`, creacion de sesion `200`, envio de mensaje `201`, lectura `200` y cookie `multiogar_chat_visitor` presente.
- Smoke de permisos 2026-08-26: `/admin/usuarios` `200`; `GET /api/admin/users` sin bearer token `401` sin filtrar cuentas.
- El registro de QA (`chat_b40d85da7fd59f9d756eb531264847b3a6d43f6c`) y su mensaje fueron eliminados despues de la prueba.
- La cuenta administradora real `andres.san1404@gmail.com` ya tiene el custom claim `superadmin`; los nuevos usuarios deben iniciar sesión o existir en Firebase antes de asignarles rol.
